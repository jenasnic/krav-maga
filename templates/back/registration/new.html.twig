{% extends 'back.html.twig' %}

{% block body %}

    <h2>{{ 'back.registration.new.title'|trans }}</h2>

    <div class="form-wrapper">
        {{ form_start(form) }}
            {{ form_rest(form) }}
            <div class="flex flex--content-end">
                <button type="submit" class="button">{{ 'global.validate'|trans }}</button>
            </div>
        {{ form_end(form) }}
    </div>

{% endblock %}
